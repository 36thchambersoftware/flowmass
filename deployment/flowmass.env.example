# Copy this to /etc/flowmass/flowmass.env and edit values.
# Keep this file readable only by the service user.

# Cardano / Blockfrost
# Note: Do NOT place inline comments on the same line as a value.
# Systemd EnvironmentFile lines are parsed literally and comments become part of the value.
# Example: use a separate comment line starting with '#' above the variable.
BLOCKFROST_API_KEY=
# Choose 'mainnet' or 'preprod'
CARDANO_NETWORK=mainnet
# Testnet magic for preprod (only used when CARDANO_NETWORK=preprod)
TESTNET_MAGIC=1

# Path to the running cardano-node socket file. This must be set for the
# service to call `cardano-cli` (no inline comments on this line).
CARDANO_NODE_SOCKET_PATH=/path/to/node.socket

# Engine configuration
MONITOR_ADDRESS=
POLICY_ID=
SCRIPT_FILE=./policy.script
METADATA_FILE=./metadata.json
STATE_FILE=/var/lib/flowmass/flowmass.state
MINT_PRICE=27000000

# Signing key (make sure this is readable only by service user)
SIGNING_KEY_FILE=/etc/flowmass/signing.skey
